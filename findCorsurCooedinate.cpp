#include <opencv2/highgui.hpp>

void mouse_callback(int  event, int  cursor_X, int  cursor_Y) {
    if (event == EVENT_MOUSEMOVE)
        //cout << "(" << cursor_X << ", " << cursor_Y << ")" << endl;
    return;
}